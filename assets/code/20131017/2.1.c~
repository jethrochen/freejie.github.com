#include <stdio.h>
#include <limits.h>

struct KeyNum {
    int low;
    int high;
    int maxsum;
};

void findMaxCrossingSubarray(int array[], int left, int right, int mid)
{
    int leftsum = MIN_INT;
    int rightsum = MIN_INT;
    int i, j;
}

void findMaxmimuxSubarray(int array[], int low, int high)
{
    int left;
    int right;
    int mid;

    if (len <= 0)
        return;
    mid = (left + right) / 2;
    findMaxmimumSubarray(array, left, mid);
    findMaxmimumSubarray(array, mid+1, right);


}

int main(void)
{
    struct KeyNum *key;


}
